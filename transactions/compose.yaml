services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=transactions_db'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=secret'
      - 'MYSQL_USER=myuser'
    ports:
      - '3306'
